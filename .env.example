# 复制这个文件为 .env，并填入真实的值（.env 不会被提交到 git，是安全的）

# 博客公开访问的网址。
# 本地开发保持 http://localhost:3000；
# 部署后替换为实际域名，例如 https://my-blog.vercel.app（Vercel）或 https://my-blog.netlify.app（Netlify）。
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# 数据库连接串。本地开发可以用 `npx prisma dev` 自动生成一个；
# 正式上线时去 https://neon.tech 免费创建一个 Postgres 数据库，把连接串填在这里。
# 如果 Vercel 和 Netlify 共用同一个数据库，两个平台填同一个连接串即可（注意去掉 channel_binding 参数）。
DATABASE_URL=""

# 后台登录密码，自己设置一个记得住但别人猜不到的密码。
ADMIN_PASSWORD=""

# 加密后台登录会话用的随机字符串，本地终端运行 `openssl rand -base64 32` 生成一个填进来即可。
SESSION_SECRET=""

# 图片/视频云存储令牌（Vercel Blob，图片统一存在这里，Vercel 与 Netlify 共用）。
# 在 Vercel 控制台创建 Blob Store 后即可拿到 BLOB_READ_WRITE_TOKEN：
# - 部署到 Vercel：创建 Blob Store 后 Vercel 会自动注入，也可手动粘贴到环境变量。
# - 部署到 Netlify：把同一个令牌手动粘贴到 Netlify 的环境变量里，上传功能即可在两平台通用。
# 本地开发时留空会禁用后台上传图片/视频功能，其他功能不受影响。
BLOB_READ_WRITE_TOKEN=""
